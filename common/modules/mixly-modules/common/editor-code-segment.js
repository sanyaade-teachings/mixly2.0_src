goog.loadJs('common', () => {

goog.require('Mixly.EditorBase');
goog.provide('Mixly.EditorCodeSegment');

const {
    EditorBase
} = Mixly;

class EditorCodeSegment extends EditorBase {
    constructor() {
        
    }
}

Mixly.EditorCodeSegment = EditorCodeSegment;

});